{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "java -jar target/oms-backend-1.0.0.jar",
    "healthcheckPath": "/api/auth/login",
    "healthcheckTimeout": 120
  }
}
